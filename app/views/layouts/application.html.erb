<!DOCTYPE html>
<html>
  <head>
    <title>Gembook</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= favicon_link_tag asset_path('favicon-32x32.png') %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Ubuntu:wght@400;700&display=swap" rel="stylesheet">
  </head>

  <body>
    <nav>
    <% if user_signed_in? %>
      <%= link_to "Home", root_url %>
      <%= link_to "Users", users_path %>
      <% if current_user.received_notifications.unread.any? %>
        <%= link_to display_notifications(current_user), notifications_path %>
      <% else %>
        <%= link_to "Notifications", notifications_path %>
      <% end %>
      <span>logged in as </span>
      <%= render partial: 'users/user_icon', locals: { user: current_user } %>
      <%= link_to current_user.full_name, edit_user_registration_path %>
      <%= link_to "My Profile", '/profile' %>
      <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
    <% else %>
      <%= link_to "Sign in with Github", user_github_omniauth_authorize_path, method: :post %>
      <%= link_to "Sign in", new_user_session_path if !current_page?(action: 'new' )%> 
    <% end %>
    </nav>
    <div id="flash-message">
      <% flash.each do |message_type, message| %>
      <%= content_tag(:p, message, class: "notification #{message_type}") %>
    <% end %>
    </div>
    <%= yield %>
    <%= debug(params) if Rails.env.development? %>
  </body>
</html>
