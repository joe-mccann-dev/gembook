<!DOCTYPE html>
<html>
  <head>
    <title>RailsFacebook</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <nav>
    <% if user_signed_in? %>
      <span>logged in as <%= current_user.first_name %></span>
      <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
      <% if current_user.received_notifications.unread.any? %>
        <%= link_to "#{current_user.received_notifications.unread.count } New Notifications", notifications_path %>
      <% else %>
        <%= link_to "Notifications", notifications_path %>
      <% end %>
    <% else %>
      <%= link_to "Sign in", new_user_session_path if !current_page?(action: 'new' )%> 
    <% end %>
    </nav>
    <div id="flash-message">
      <% flash.each do |message_type, message| %>
      <%= content_tag(:p, message, class: "notification #{message_type}") %>
    <% end %>
    </div>
    <%= yield %>
  </body>
</html>
