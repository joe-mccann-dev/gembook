<%= button_to "Decline",
                friendship_path(@friendships[notification.sender.id]),
                method: :put,
                params: { notification: { id: notification.id }, friendship: { status: 'declined' } },
                class: "button decline-request",
                data: { action: "click->notification-count#decrementNotificationCount" },
                id: "decline-sender-#{notification.sender.id}-request" %>